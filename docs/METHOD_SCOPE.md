# Method Scope

The current manuscript MM-RVD model is the A9-selected R6 implementation: temporal low-dimensional covariance-aware prototype decoding with observed-only statistics and mask-matched reference construction.

Historical model variants and discarded development experiments are not included as scientific result sources in this public export. Large fitted states and raw neural responses are represented by manifests and hashes rather than committed as ordinary GitHub files.
